package models

//import com.phasmidsoftware.number.model._

/**
 * @author scalaprof
 */
object NumberMill {

  //  val conv: String=>Try[Number] = NumberMill.valueOf
  //  val lookup: String=>Option[Number] = NumberMill.constants.get
  //  implicit val store = Map[String,Number]()
  //  implicit val parser = new ExpressionParser[Number](conv,lookup)
  //  // CONSIDER making this a class NumberMill
  //  def apply(): Mill[Number] = new Mill(Stack[Number]()) {
  //    def apply(s: String): Try[Number] = NumberMill.valueOf(s)
  //  }
  //  def valueOf(s: String): Try[Number] = Try(Number(s))
  //  val constants = Map("e"->Number(1, E), "pi"->Number(1, Pi))
}